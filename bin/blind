#! /usr/bin/env ruby

require_relative '../src/blind'
require_relative '../src/error/blind_error'

begin
  Blind.new.call *ARGV
rescue Error::BlindError => error
  usage = 'Usage: blind [<directory>][--dry-run][--help]'
  abort "#{usage}\n\nError: #{error.message}"
end
